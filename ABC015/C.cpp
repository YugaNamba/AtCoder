#include <bits/stdc++.h>
#define rep(i,n) for (int i = 0; i < (n); i++)
#define all(v) v.begin(), v.end()
#define rAll(v) v.rbegin(), v.rend()
using namespace std;

int main() {
  int n, k;
  cin >> n >> k;
  int t[n][k];
  rep(i, k) {
    rep(j, n) {
      cin >> t[j][i];
    }
  }

  rep(i, k) {
    
  } 

  cout << n << endl;
  return 0;
}