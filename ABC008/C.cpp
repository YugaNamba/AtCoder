#include <bits/stdc++.h>
#define rep(i,n) for (int i = 0; i < (n); i++)
#define all(v) v.begin(), v.end()
using namespace std;

int main() {
  int n;
  cin >> n;
  int c[n];
  rep(i, n) {
    cin >> c[i];
  }
  
  cout << n << endl;
  return 0;
}